adaptogen (C:/dev/dstate) session 2026-06-18g: CORRECTION to session-f's 'npx access confirmed' -- that witness was `node src/cli.js`, NOT the package runner. Re-tested over the REAL runners against the published npm tarball (registry latest 0.1.5, gitHead e53c6f5, release-bot bump): `npx -y adaptogen@latest describe` -> exit 0 valid manifest; `npx -y adaptogen@latest remember n --label hi --db :memory: --json` -> exit 0 valid node JSON; `bunx adaptogen@latest describe` -> exit 0 valid manifest (bunx stderr is just resolve/lockfile progress, not error). So npx AND bunx both run the published bin: npx-on-node proves the published shebang is node. Published 0.1.5 engines={bun:>=1.1.0} lacks a node entry; local main adds node>=18 but npm engines is advisory so it does not gate the runner -- no republish needed. Lesson: to witness 'npx/bunx access', run `npx -y <pkg>@latest <cmd>` / `bunx <pkg>@latest <cmd>` from a tmpdir, never `node src/cli.js` (that only proves the source runs under node, not that the published package-runner path works).