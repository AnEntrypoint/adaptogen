adaptogen (was dstate, C:/dev/dstate) is published on npm as 'adaptogen' (latest 0.1.1) and lives at github.com/AnEntrypoint/adaptogen (org AnEntrypoint, pushed by gh user lanmower; repo was archived+Solidity, unarchived via gh api archived=false then force-pushed, overwriting old content). main is the permanent upstream. CI .github/workflows/publish.yml publishes on every push to main: bun test + bun test.js, npm version patch, npm publish via repo secret NPM_TOKEN (already set), then git push HEAD:main --follow-tags with a [skip ci] message to avoid recursion. GOTCHA: a run: value containing ': ' (colon-space) like the npm version -m message breaks YAML with 'mapping values not allowed' and fails the run in 0s — use a block scalar (run: |). The JS class is exported as both DState and Adaptogen (alias). bin renamed to 'adaptogen', default db ./adaptogen.db.