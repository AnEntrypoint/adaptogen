## Resolved mutable: m-bun-sqlite

exec `bun -e require('bun:sqlite')` -> {bun_version:1.3.8, sqlite_version:3.51.2}. bun:sqlite available; library targets bun runtime. Note: exec_js spool verb runs under node and cannot import bun: scheme, so bun-native witnesses use `bun -e`.