adaptogen (C:/dev/dstate) agent-fluency pass 2026-06-18: added two pure-composition verbs on DState (no new event types). plan(spec={nodes,transitions,deps,cursor}) is an ATOMIC declarative workflow builder: validates the whole spec (id charset, in-spec dup, payload size, endpoint-resolves-if-existing-or-in-spec, guard compile, weight, batch dependency-acyclicity via depAdjacency+hasPath incremental, cursor active) against a dry projection BEFORE any append, so a failure writes ZERO events and names the offending item by index; commit order nodes->transitions->deps->cursor cannot fail post-validation (acyclic subset stays acyclic). orient(vars) returns one situational snapshot {cursor,suggestions,legalMoves,blocked[{to,reasons}],ready,violations,integrity_ok,recent,seq,ftsEnabled,tunables,done}; imports validate+history into engine.js (no cycle). Exposed on CLI (plan --spec <json>, orient; usage 'compose:' section), manifest.js (verbs.compose + build_a_workflow/orient_then_act patterns), README. test.js now 196/200 lines, 70 assertions. Commit 713c51c pushed to main.