adaptogen (C:/dev/dstate) agent-fluency pass 2026-06-18d (commit 8144aa1, pushed main): full-codebase audit -> 7 grounded improvements. (1) PERF: store.js readEvents now takes {limit} for tail reads (ORDER BY seq DESC LIMIT N then reverse to asc); engine.js recentTransitions uses limit:n, cleanStreak pages backward in 256-event windows with early-exit on violation/threshold -- removed O(seq)-per-step full-log scans on long sessions. Full-log callers (recover/portability/history) pass no limit, unchanged. (2) test.js (200-line ceiling, now 73 assertions): surface-sync witness Object.values(MANIFEST.verbs).flat().every(v=>typeof ds[v[0]]==='function') permanently catches describe()/impl drift; + readEvents limit order assertion; + guard unknown-escape rejection. (3) manifest.js: added zonesOf to verbs.zones (was a real method missing from describe()). (4) README: added Compose section (plan/orient) + setStatus to verb surface. (5) cli.js: import guards JSON.parse (clean err+exit1, no raw throw); added render/tunables/set-tunable/repair/verify subcommands for full manifest parity from shell. (6) guard.js: ESCAPES whitelist {\\ \" \' n r t}, rejects unknown escapes with GuardParseError. AUDIT NOTE: subagent 'critical' claims that were FALSE POSITIVES on verification -- plan() unsafe-unwrap (safe by construction, validated-then-commit), pReward NaN poison (ensureStat guarantees row + ema_reward NOT NULL DEFAULT 0), intuition epsilon double-rand (correct), graph.js cycle-path dup (intentional closed-loop repr), guard array unbounded (MAX_EXPR_LEN=2000 bounds it), ucbC=0 / null-enforcement-no-demote (by design). Codebase is mature; density past real witnesses would be churn.