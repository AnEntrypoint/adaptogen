adaptogen (C:/dev/dstate, npm latest 0.1.5) maturity-vet pass: a 145-row exhaustive test matrix (auto-expanded at PLAN) was honored with 5 new themed test files (edge-degenerate, security-contract, durability-evolve, agent-patterns, intuition-policy) + cli contract cases = 149 unit tests green. Found+fixed TWO real bugs: (1) recall({text}) with FTS5 metacharacters (; ( ) : *) threw uncaught SqliteError -> now wraps query as a literal FTS5 phrase (double-quote, double embedded quotes); (2) verifyIntegrity()/validate() reported false HashChainBreak after compact() trimmed the log prefix -> now anchors on first retained event (genesis check kept only when seq==1). GOTCHA for future: UCB explore term is 0 when totalVisits==0 (log(1)=0), seed a visit before asserting explore>0. The publish-on-push CI runs the full suite on ubuntu (cross-platform witness). bun test = 149 pass, test.js OK, bench flat.