dstate (C:/dev/dstate) session 2026-06-18: cleaned up post-JS-migration. package.json now points at src/*.js (was broken src/*.ts in main/module/bin/scripts; dropped tsc/typescript). CLI (src/cli.js) gained mutation subcommands remember/get/recall/link/depend/unlink/enforce/cursor/transition/reward via a new --flag parser + emitResult, so an agent can drive a full session by shelling out (was inspect-only). manifest.js describe() now includes getStat and a tunables{defaults,ranges} block. New invariant validate.js DependencyToDeadNode (active dependent on a dead prereq), fixable; evolve.js selfIterate now prunes a gc'd node's edges. link() rejects non-finite/negative weight. Tests: test/cli.test.js (spawns real cli over temp db) + validate.test.js cases. 73 unit pass, bun test.js OK, bench under budget. No git remote configured -> push blockedBy external (commit 5ebf4fc local only).